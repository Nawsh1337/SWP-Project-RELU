Project which uses python to visualize the output of RELU in 3D, where it is possible to change the weights and biases.
Can also use the pretrained models that compute the max, min, and avg for 3 inputs.
